export function Menu() {

    function load(callback) {
        callback();
    }

    function update(dt) {
        
    }

    function draw() {
        clearBackground(BLUE);
        
    }

    function unload() {
        
    }

    return {
        load,
        update,
        draw,
        unload
    }
}